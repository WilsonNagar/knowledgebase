name: Deploy to Server

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch: # Allows manual trigger from GitHub UI

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment Info
        run: |
          echo "🚀 Deployment triggered!"
          echo ""
          echo "This repository uses webhook-based deployment."
          echo "GitHub automatically sends a webhook to your server when code is pushed."
          echo ""
          echo "📋 To view deployment logs, run on your server:"
          echo "   ssh wilsonnagar@wilson-home 'sudo journalctl -u webhook.service -f'"
          echo ""
          echo "✅ Deployment is handled automatically by the webhook server."
          echo "   No action needed - the webhook will deploy your changes."
